# src/jarvis/core/__init__.py
#
# Marks "core" as a sub-package.
#
# This folder is reserved for the "brain" of JARVIS: the conversation engine,
# the AI reasoning logic, command routing (deciding what an instruction means
# and which module should handle it), and the wake-word detection loop.
# It is intentionally empty in Phase 1 — we build it in Phases 3-8.
